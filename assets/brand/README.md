# Moved

Brand assets now live under `../images/brand/`. This folder is kept only
to avoid breaking in-flight references; it is empty and can be deleted
once nothing points at the old path.

See `../../IMAGE_ASSET_PLAN.md` for the current asset layout.
